- sort_dir = @sorted_customers.sort_column == column_name && @sorted_customers.sort_direction == :asc ? :desc : :asc
= form_tag customers_path, method: :get do |f|
  = hidden_field_tag :sort_column, column_name
  = hidden_field_tag :sort_direction, sort_dir
  = submit_tag column_name.to_s.humanize
